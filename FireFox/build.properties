assemble.content.dir=${assemble.dir}/content
assemble.locale.dir=${assemble.dir}/locale
assemble.skin.dir=${assemble.dir}/skin
deploy.fileName=${application.name}-${application.version}.xpi

# Change the following to allow multiple extensions to be installed together...
# Please ensure that only valid JavaScript naming characters are allowed!
application.package.name=alfresco_activities
application.id.prefix=alfresco_activities_